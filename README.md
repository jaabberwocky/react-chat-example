# Weekend project with React + Socket.io

Fun weekend project to learn about integrating the amazing `socket.io` and `reactjs`. Implementation is fullstack and split into both `client` (frontend) and `server` (backend).